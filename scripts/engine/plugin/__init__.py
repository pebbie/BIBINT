"""
site handling interface : 
- plugin capabilities : url matcher, http header matcher, html content matcher, service availability (search)
- query search (free text) : return a list of url
- specific query search (e.g. author) : return a list of url
- interpret a page given url : output structured information (author profile, bibliography, document metadata)
"""
